"use client";

// Atmospheric intro — the star stands alone. The DOM hero overlay carries the
// portfolio content (name, pitch, status, CTAs). We intentionally do NOT render
// floating 3D text here anymore: it was unreadable and duplicated the overlay.
export default function IntroHalo() {
  return null;
}
